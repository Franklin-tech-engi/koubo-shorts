# 口播 → 竖屏短视频 · 自动化流水线

把你录好的口播原片，自动处理成可以直接发布的竖屏短视频。
全部用**开源工具**，**零 API 费用**。

处理流程：**去口水/去静音 → 语音转字幕 → 烧简单字幕 → 竖屏化(9:16)**

---

## 一、第一次使用（只需做一次）

打开「终端」App，把下面这行粘进去回车（先 `cd` 到本文件夹）：

```bash
cd "你放这个文件夹的路径"
bash install.sh
```

它会自动装好 ffmpeg 和字幕识别工具。中途可能让你输一次 Mac 密码，正常。

> 如果你用 Claude Code，直接在本文件夹里让它帮你执行 `bash install.sh` 即可。

---

## 二、每天怎么用（三步）

1. 把要处理的原片**拖进 `input/` 文件夹**（可以一次放多条，批量处理）。
2. 在本文件夹运行：

   ```bash
   python3 make_shorts.py
   ```

3. 剪好的竖屏成片自动出现在 **`output/` 文件夹**。

> 第一次运行会下载字幕模型（约几百 MB，只下这一次）。之后就很快了。

---

## 三、想调效果？改 `make_shorts.py` 顶部的 CONFIG 就行

| 配置项 | 作用 | 常改成 |
|---|---|---|
| `language` | 字幕语言 | 中文 `"zh"`，英文 `"en"` |
| `whisper_model` | 识别模型 | 求快 `"base"`；求准 `"medium"`；默认 `"small"` |
| `reframe_mode` | 竖屏方式 | `"blur_pad"`(不裁人，推荐) / `"crop"`(画面更满) |
| `font_size` | 字幕大小 | 数字越大字越大 |
| `subtitle_margin_v` | 字幕离底部距离 | 数字越大越靠上 |
| `silence_min_dur` | 多长的静音才剪 | 想保留更多停顿就调大 |
| `max_chars_per_line` | 每行字幕最多几个字 | 中文一般 12~16 |

改完保存，重新运行即可。

---

## 四、常见问题

**字幕是方块/乱码？**
说明系统缺中文字体（Mac 一般自带 PingFang SC，不会有这问题）。可把 CONFIG 里的
`font_name` 换成你系统里存在的字体名。

**竖屏后人被裁掉了？**
把 `reframe_mode` 改成 `"blur_pad"`（默认就是它，画面完整、上下用模糊背景填充）。

**字幕识别不准？**
把 `whisper_model` 从 `"small"` 调成 `"medium"`（更准但更慢）。

**想不烧字幕、只要竖屏 + 去静音？**
告诉我（或 Claude Code），几行就能加个开关。

---

## 五、这套东西依赖什么

- **ffmpeg** —— 视频裁剪、拼接、竖屏化、烧字幕
- **faster-whisper** —— 开源语音转文字（本地跑，不上传、不花钱）
- **Python 3**

全部开源、本地运行，你的视频不会上传到任何第三方。
